export declare enum ExtensionControlSource {
    Extension = 1,
    Metadata = 2
}
export declare function isExtensionSource(source: ExtensionControlSource): boolean;
export declare function isMetadataSource(source: ExtensionControlSource): boolean;
