import { OnDestroy } from '@angular/core';
import { Observable, ReplaySubject, Subscription } from 'rxjs';
export declare class BaseService implements OnDestroy {
    protected isDeleted: boolean;
    readonly destroy$: ReplaySubject<boolean>;
    ngOnDestroy(): void;
    takeUntilDestroy<T>(): (source: Observable<T>) => Observable<T>;
    protected timerUnlessDestroyed(cb: () => void, delay?: number): Subscription;
}
