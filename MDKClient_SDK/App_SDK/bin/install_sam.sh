#!/bin/bash

echo 
echo The install_sam script has been replaced by create-client-dev.command.
echo To install SAM in your dev environment, run bin/build_sim.sh or bin/build_device.sh in the SAM repository.
echo "This step is only needed to set up <SAM>/src/SAM.mdkproject, so if this project is already set up, you can skip this step."
echo "Next run bin/create-client-dev.command -m <SAM>/src/SAM.mdkproject."
echo "That's it! You can still use bin/remove_sam.sh to clean up the changes made for the SAM app."
echo "Run bin/create-client-dev.command --help for more information."
echo 
